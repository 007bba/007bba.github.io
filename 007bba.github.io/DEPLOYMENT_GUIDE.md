# Jekyll 博客部署指南

## 项目概述

这是一个基于 Jekyll 的静态博客，已经配置好可以直接部署到 GitHub Pages。

## 项目结构

```
007bba.github.io/
├── _config.yml          # Jekyll 配置文件
├── Gemfile              # Ruby 依赖管理
├── index.md             # 首页
├── about.md             # 关于页面
├── README.md            # 项目说明
├── _layouts/            # 布局模板
│   ├── default.html     # 默认布局
│   └── post.html        # 文章布局
├── _posts/              # 博客文章
│   └── 2024-10-30-welcome-to-jekyll.md
├── assets/              # 静态资源
│   └── css/
│       └── style.css    # 样式文件
└── DEPLOYMENT_GUIDE.md  # 本文件
```

## 部署到 GitHub Pages

### 方法一：自动部署（推荐）

1. **推送代码到 GitHub**
   ```bash
   git add .
   git commit -m "初始化 Jekyll 博客"
   git push origin main
   ```

2. **启用 GitHub Pages**
   - 进入 GitHub 仓库设置
   - 找到 "Pages" 选项
   - 选择 "Deploy from a branch"
   - 选择 `main` 分支，`/ (root)` 文件夹
   - 保存设置

3. **等待部署完成**
   - GitHub 会自动构建并部署你的网站
   - 访问地址：https://007bba.github.io

### 方法二：本地构建后部署

1. **安装 Ruby 和 Jekyll**
   ```bash
   # Windows 使用 RubyInstaller
   # 安装完成后运行
   gem install jekyll bundler
   ```

2. **本地构建**
   ```bash
   cd 007bba.github.io
   bundle install
   bundle exec jekyll serve
   ```

3. **访问本地预览**
   - 打开浏览器访问 http://localhost:4000

## 写作新文章

1. **创建新文章**
   - 在 `_posts` 目录下创建新文件
   - 文件名格式：`YYYY-MM-DD-标题.md`

2. **文章模板**
   ```markdown
   ---
   layout: post
   title: "文章标题"
   date: 2024-10-30 10:00:00 +0800
   categories: [分类1, 分类2]
   ---
   
   # 文章内容
   
   使用 Markdown 语法编写内容...
   ```

## 自定义配置

### 修改网站信息
编辑 `_config.yml` 文件：

```yaml
title: "你的博客标题"
description: "博客描述"
author:
  name: "你的名字"
  email: "你的邮箱"
```

### 修改样式
编辑 `assets/css/style.css` 文件来自定义外观。

## 常用命令

```bash
# 本地开发
bundle exec jekyll serve

# 构建网站
bundle exec jekyll build

# 清理并重新构建
bundle exec jekyll clean && bundle exec jekyll build
```

## 故障排除

### 常见问题

1. **构建失败**
   - 检查 `_config.yml` 语法是否正确
   - 确保所有 YAML 前置信息格式正确

2. **样式不生效**
   - 检查 CSS 文件路径
   - 清除浏览器缓存

3. **文章不显示**
   - 检查文件名格式是否正确
   - 确保 YAML 前置信息完整

## 进阶功能

### 添加评论系统
可以使用 Disqus 或 Gitalk 添加评论功能。

### 添加搜索功能
集成 Algolia 或 Lunr.js 实现搜索。

### 自定义域名
在 GitHub Pages 设置中添加自定义域名。

## 技术支持

如有问题，请参考：
- [Jekyll 官方文档](https://jekyllrb.com/)
- [GitHub Pages 文档](https://docs.github.com/en/pages)

---

**祝你的博客顺利上线！** 🚀